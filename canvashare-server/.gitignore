# Compiled files and directories
/target/

# Remove Cargo.lock from gitignore if creating an executable, keep it if creating a library
Cargo.lock

# These are backup files generated by rustc
**/*.rs.bk

# Generated files from `cargo doc`
/doc/

# Dependency directories (remove the comment below if you want to include them in your git repo)
#/Cargo.lock
